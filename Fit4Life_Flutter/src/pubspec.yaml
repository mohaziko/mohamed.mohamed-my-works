name: src
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7
  
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  scrolling_buttons_bar: ^0.0.1

  # HIVE (LOCAL DB)
  hive_ce: ^2.18.0
  hive_ce_flutter: ^2.3.4
  image_picker: ^1.2.1
  share_plus: ^12.0.1
  url_launcher: ^6.3.2
  qr_flutter: ^4.1.0
  flutter_local_notifications: ^19.5.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # HIVE (LOCAL DB)
  hive_generator: ^1.1.3
  build_runner: ^2.1.11

  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true
